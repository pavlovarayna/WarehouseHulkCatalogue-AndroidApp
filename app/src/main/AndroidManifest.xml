<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rt.warehousehulk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity android:name=".views.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".views.contacts.ContactsActivity" />
        <activity android:name=".views.contacts.EmailActivity" />
        <activity android:name=".views.promotions.PromotionActivity" />
        <activity android:name=".views.products.ProductListActivity" />
        <activity android:name=".views.products.DrinksListActivity" />
        <activity android:name=".views.products.FoodLisActivity" />
        <activity android:name=".views.products.DomesticsListActivity"></activity>
    </application>

</manifest>